document.addEventListener('DOMContentLoaded', () => {
    // Inicializar módulos
    // Las funciones se importarán globalmente al estar en scripts separados por ahora
    // En un futuro se podría usar ES Modules con imports
});
